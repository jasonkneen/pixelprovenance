# AGENTS.md — decoder/

PNG CLI and Node decode API (`pixelprovenance/decode`).

- Read /AGENTS.md first.
- `--registry` is required. Do not hard-code component paths.
- Keep CLI JSON/text/exit-status behavior covered by `scripts/verify-package.mjs`.
